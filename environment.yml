# This is an environment for running git commands and other tools locally.
name: dtu-denovo-sequencing
channels:
  - default
dependencies:
  - python=3.8.3
  - pip
  - setuptools
  - pip:
      - -r requirements-dev.txt
      - -r requirements.txt
