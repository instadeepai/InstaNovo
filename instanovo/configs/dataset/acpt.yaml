# ACPT dataset

defaults:
  - default
  - _self_

train_path: /mnt/instanovo-ceph/identity_splits_parquet/acpt/*-train-*.parquet # Unshuffled
valid_path: /mnt/instanovo-ceph/identity_splits_parquet/validation_subsets/dataset-acpt-valid-0000-0001.parquet

preshuffle_across_shards: True
